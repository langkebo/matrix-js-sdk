// Auto-generated by contract:codegen. Do not edit manually.
// Source: docs/api-contract/presence.md

// ─── Presence State ───────────────────────────────────────────

/** Possible presence states */
export type PresenceState = "online" | "offline" | "unavailable" | "away" | "busy";

/** Presence state for a user (returned by GET /presence/{userId}/status) */
export interface PresenceUserState {
    /** Current presence state */
    presence: PresenceState;
    /** Status message (optional) */
    status_msg?: string;
    /** Milliseconds since the user was last active */
    last_active_ago?: number;
    /** Whether the user is currently active (server-determined) */
    currently_active?: boolean;
}

/** A presence event for a user (includes user_id) */
export interface PresenceUser {
    /** User ID */
    user_id: string;
    /** Current presence state */
    presence: PresenceState;
    /** Status message (optional) */
    status_msg?: string;
    /** Milliseconds since the user was last active */
    last_active_ago?: number;
    /** Whether the user is currently active */
    currently_active?: boolean;
}

// ─── Set Presence ─────────────────────────────────────────────

/** Request body for PUT /presence/{userId}/status */
export interface SetPresenceRequest {
    /** New presence state */
    presence: PresenceState;
    /** Status message (optional) */
    status_msg?: string;
}

// ─── Get Presence ─────────────────────────────────────────────

/** Response from GET /presence/{userId}/status */
export type GetPresenceResponse = PresenceUserState;

// ─── Presence List ────────────────────────────────────────────

/** Response from POST /presence/list or GET /presence/list/{userId} */
export interface PresenceListResponse {
    /** List of presence events */
    presences: PresenceUser[];
}

/** Request body for POST /presence/list (subscribe) */
export interface SubscribePresenceRequest {
    /** User IDs to subscribe to */
    subscribe: string[];
}

/** Request body for POST /presence/list (unsubscribe) */
export interface UnsubscribePresenceRequest {
    /** User IDs to unsubscribe from */
    unsubscribe: string[];
}
