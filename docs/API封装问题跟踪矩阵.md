# API封装问题跟踪矩阵

> 格式参考 JIRA，责任人按角色拆分为 `前端` / `后端` / `QA`。

| Key | Summary | 等级 | 责任人 | 修复时限 | 截止日期 | 验证标准 | 当前状态 |
|-----|---------|------|--------|---------|---------|---------|---------|
| FE-API-001 | 清理剩余双前缀 URL 组装点（appservice/guest/saml/voice 等） | 致命 | 前端 | 3天 | 2026-04-06 | 所有 URL 组装测试通过，真实请求不再出现重复前缀 | In QA |
| FE-API-002 | 为 SDK 增加 HTTPS 强制策略与显式本地开发豁免开关 | 致命 | 前端 | 3天 | 2026-04-06 | 非 HTTPS 默认拒绝，开发环境需显式开启 | Open |
| FE-API-003 | 重构 `SpaceManager`，补齐 `/spaces/*` 契约封装 | 严重 | 前端 | 7天 | 2026-04-10 | `space.md` 高价值端点具备封装与测试 | Open |
| FE-API-004 | 补齐 `RoomSummaryManager` 写路径与维护接口 | 严重 | 前端 | 7天 | 2026-04-10 | `room-summary.md` 写/维护端点补齐并通过回归测试 | Open |
| FE-API-005 | 统一 Manager 声明、运行时挂载、默认初始化机制 | 严重 | 前端 | 7天 | 2026-04-10 | `declaredOnly` / `implementedOnly` 清零或有明确废弃清单 | Resolved |
| FE-API-006 | 清理 `device-management` 等重复封装，形成唯一入口 | 一般 | 前端 | 14天 | 2026-04-17 | 重复 Manager 下线或标记废弃，类型声明一致 | Open |
| FE-API-007 | 建立统一 Matrix 标识输入校验层 | 一般 | 前端 | 14天 | 2026-04-17 | 用户 ID / 房间别名 / 事件类型校验覆盖单测 | Open |
| FE-API-008 | 建立 GET 协商缓存与初始化请求合并策略 | 一般 | 前端 | 14天 | 2026-04-17 | ETag/Last-Modified 或等效缓存方案生效；关键启动请求有并发编排 | Open |
| FE-API-009 | 收敛 `extraEndpoints`：按“补合同 / 改实现 / 待决策”三类闭环 | 严重 | 前端 | 7天 | 2026-04-10 | 三分类清单落文档，且每类至少完成一轮修正或决策记录 | In Progress |
| BE-API-001 | 补充限流规则元数据（QPS/并发/burst）到契约文档或服务配置导出 | 一般 | 后端 | 14天 | 2026-04-17 | 前端可依据契约读取限流信息 | Open |
| QA-API-001 | 为每个高优端点建立成功/错误/边界用例矩阵 | 严重 | QA | 7天 | 2026-04-10 | 成功路径、错误码、边界值均有自动化用例 | Open |
| QA-API-002 | 在真实 homeserver 环境补齐 HTTPS/GZIP/chunked 抓包验证 | 一般 | QA | 14天 | 2026-04-17 | 抓包报告可复现，覆盖 TLS/压缩/分块传输 | Open |

## 验证说明

- 致命：3天内完成，必须通过测试用例 + 回归测试
- 严重：7天内完成，必须通过测试用例 + 回归测试
- 一般：14天内完成，必须通过测试用例 + 回归测试
