# matrix-js-sdk 漏洞关闭追踪（2026Q2）

## 1. 高危漏洞治理策略

- 高危漏洞 SLA：7 天内关闭。
- 中危漏洞 SLA：30 天内关闭。
- 默认策略：通过 `pnpm overrides` 先收敛传递依赖，再评估主依赖升级。

## 2. 当前高危链路与处置

| 组件        | 风险摘要                    | 处置策略                 | 目标版本            | 状态   |
| ----------- | --------------------------- | ------------------------ | ------------------- | ------ |
| `vite`      | dev server 文件读取相关高危 | overrides 锁定补丁版本   | `7.3.2+`            | 进行中 |
| `rollup`    | 传递依赖高危                | overrides 提升到补丁版本 | `4.59.0+`           | 进行中 |
| `picomatch` | ReDoS 高危                  | 分支版本双轨覆盖         | `2.3.2+` / `4.0.4+` | 进行中 |
| `minimatch` | ReDoS 高危                  | 双主版本区间覆盖         | `3.1.4+` / `9.0.7+` | 进行中 |
| `flatted`   | DoS/污染相关高危            | overrides 锁定补丁版本   | `3.4.2+`            | 进行中 |

## 3. 本轮变更记录

- 已在 `package.json` 增加 `pnpm.overrides`：
    - `vite@<7.3.2 -> 7.3.2`
    - `rollup@<4.59.0 -> 4.59.0`
    - `picomatch@<2.3.2 -> 2.3.2`
    - `picomatch@>=4.0.0 <4.0.4 -> 4.0.4`
    - `minimatch@<3.1.4 -> 3.1.4`
    - `minimatch@>=9.0.0 <9.0.7 -> 9.0.7`
    - `flatted@<3.4.2 -> 3.4.2`

## 4. 验收标准

- `pnpm audit --audit-level=high` 结果中高危数量持续下降并最终归零。
- 每个豁免项需有到期时间和责任人，逾期自动升级为 P0 风险。

## 5. 最新结果（2026-04-09）

- `pnpm audit --json`：`high=0`、`moderate=0`、`critical=0`。
- 结论：依赖漏洞基线已达到“可发布安全门禁”要求，后续进入持续维护阶段（按 SLA 月度巡检）。
